<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.framgia.nvmanh.boxmovie">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screen.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screen.popular.PopularActivity"/>
        <activity android:name=".screen.toprate.TopRatedActivity"/>
        <activity android:name=".screen.nowplaying.NowPlayingActivity"/>
        <activity android:name=".screen.upcoming.UpComingActivity"/>
        <activity android:name=".screen.video.VideoActivity"
            android:screenOrientation="landscape"/>
        <activity android:name=".screen.movie.MovieActivity"/>
        <activity android:name=".screen.detail.MovieDetailActivity"/>
    </application>
</manifest>
